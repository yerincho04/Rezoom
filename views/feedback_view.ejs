<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title><%= feedback.company %> 피드백 보기</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-200 font-sans">

  <!-- Header -->
  <%- include('partials/header', { email: typeof email !== 'undefined' ? email : null }) %>

  <!-- Main -->
  <main class="max-w-3xl mx-auto mt-10 bg-white p-8 rounded-lg shadow">
    <h1 class="text-2xl font-bold mb-4">
      <%= feedback.company %> - <%= feedback.position %> 피드백
    </h1>

    <p class="text-sm text-gray-500 mb-6">파일: <%= feedback.filename %></p>

    <div class="whitespace-pre-wrap text-gray-800 bg-gray-100 p-6 rounded-lg border border-gray-300 text-sm leading-relaxed">
      <%= feedback.full_feedback %>
    </div>

    <div class="mt-6">
      <a href="/profile" class="text-blue-600 hover:underline">← 프로필로 돌아가기</a>
    </div>
  </main>
</body>
</html>